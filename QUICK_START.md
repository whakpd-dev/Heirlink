# ⚡ Быстрый старт HeirLink

## За 5 минут до первого запуска

### Шаг 1: Установка зависимостей (2 минуты)

```bash
# Backend
cd /Volumes/alim/HeirLink/backend
npm install

# Mobile
cd ../mobile
npm install
```

### Шаг 2: Настройка базы данных (1 минута)

```bash
# Запустить PostgreSQL и Redis через Docker
cd /Volumes/alim/HeirLink
docker-compose up -d postgres redis

# Подождать 5 секунд для запуска
sleep 5
```

### Шаг 3: Настройка окружения (30 секунд)

```bash
# Backend
cd backend
cp .env.example .env
# Отредактируйте .env если нужно (обычно можно оставить как есть)
```

### Шаг 4: Миграции БД (30 секунд)

```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
```

### Шаг 5: Запуск (1 минута)

**Terminal 1 - Backend:**
```bash
cd /Volumes/alim/HeirLink/backend
npm run dev
```

**Terminal 2 - Mobile (Expo):**
```bash
cd /Volumes/alim/HeirLink/mobile
npm start
```

Затем:
- Нажмите `i` для iOS симулятора
- Нажмите `a` для Android эмулятора  
- Или отсканируйте QR код в Expo Go

## ✅ Готово!

Теперь вы можете:
1. Зарегистрировать аккаунт
2. Войти в систему
3. Просматривать ленту (пока пустую)

## 📱 Запуск на телефоне

**Пошаговая инструкция:** [mobile/ЗАПУСК_НА_ТЕЛЕФОНЕ.md](./mobile/ЗАПУСК_НА_ТЕЛЕФОНЕ.md)

Кратко:
1. Один раз: `sudo launchctl limit maxfiles 65536 200000` (см. [EMFILE](#-ошибка-emfile-too-many-open-files-macos))
2. Терминал 1: `cd backend && npm run dev`
3. Терминал 2: `cd mobile && npm start` — появится **QR-код**
4. Телефон в той же Wi‑Fi → установи **Expo Go** → отсканируй QR

API с телефона подставляется автоматически (тот же хост, что и Metro).

## 🐛 Ошибка «EMFILE: too many open files» (macOS)

Если при `npm start` в папке mobile падает с такой ошибкой:

1. **Установите Watchman** (Metro будет следить за файлами через него, а не через тысячи дескрипторов):
   ```bash
   brew install watchman
   ```
2. Перезапустите: `cd mobile && npm start`.

Если Watchman поставить нельзя — один раз в терминале перед запуском:
   ```bash
   ulimit -n 65536
   cd mobile && npm start
   ```

## 🐛 Другие проблемы

Смотрите подробную инструкцию: [EXPO_SETUP.md](./EXPO_SETUP.md)

---

**Время до запуска: ~5 минут** ⏱️
