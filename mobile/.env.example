# API (используется в приложении через EXPO_PUBLIC_API_URL)
# Если бэкенд на другом компьютере — укажи его IP и порт
EXPO_PUBLIC_API_URL="http://localhost:3000"

# Для справки (приложение берёт API из config.ts / Constants)
# API_BASE_URL = EXPO_PUBLIC_API_URL + "/api"
# WS_URL = EXPO_PUBLIC_API_URL с заменой http -> ws
