# 🚀 Запуск HeirLink в Expo

## Быстрый старт

### 1. Предварительные требования

- Node.js 18+ установлен
- Expo CLI установлен глобально (опционально):
  ```bash
  npm install -g expo-cli
  ```
- Expo Go приложение на телефоне (для тестирования на реальном устройстве)
  - [iOS App Store](https://apps.apple.com/app/expo-go/id982107779)
  - [Google Play Store](https://play.google.com/store/apps/details?id=host.exp.exponent)

### 2. Установка зависимостей

```bash
# Перейти в директорию mobile
cd /Volumes/alim/HeirLink/mobile

# Установить зависимости
npm install
```

### 3. Настройка API URL

**Важно!** Для подключения к backend нужно правильно настроить URL:

#### Для iOS симулятора:
Используется `http://localhost:3000` (уже настроено по умолчанию)

#### Для Android эмулятора:
Используется `http://10.0.2.2:3000` (уже настроено по умолчанию)

#### Для физического устройства:

1. Узнайте IP адрес вашего компьютера в локальной сети:

   **macOS/Linux:**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   # Или
   ipconfig getifaddr en0
   ```

   **Windows:**
   ```bash
   ipconfig
   # Найдите IPv4 адрес вашего адаптера (обычно 192.168.x.x)
   ```

2. Отредактируйте файл `mobile/src/config.ts`:

   ```typescript
   // Для физического устройства замените:
   return 'http://localhost:3000';
   // На:
   return 'http://YOUR_IP_ADDRESS:3000';
   // Например: 'http://192.168.1.100:3000'
   ```

3. Убедитесь, что backend запущен и доступен по этому адресу

### 4. Запуск backend (обязательно!)

Перед запуском Expo приложения нужно запустить backend:

```bash
# Terminal 1: Backend
cd /Volumes/alim/HeirLink/backend

# Установить зависимости (если еще не установлены)
npm install

# Создать .env файл (если еще не создан)
cp .env.example .env

# Запустить базу данных (если используете Docker)
cd ..
docker-compose up -d postgres redis

# Применить миграции
cd backend
npm run prisma:generate
npm run prisma:migrate

# Запустить backend
npm run dev
```

Backend должен быть доступен на `http://localhost:3000/api`

### 5. Запуск Expo приложения

```bash
# Terminal 2: Mobile
cd /Volumes/alim/HeirLink/mobile

# Запустить Expo
npm start
# Или
npx expo start
```

После запуска вы увидите QR код и меню:

```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

› Press r │ reload app
› Press m │ toggle menu
```

### 6. Варианты запуска

#### Вариант A: На симуляторе/эмуляторе (рекомендуется для разработки)

**iOS симулятор:**
```bash
npm start
# Затем нажмите 'i' в терминале
# Или
npm run ios
```

**Android эмулятор:**
```bash
npm start
# Затем нажмите 'a' в терминале
# Или
npm run android
```

**Требования:**
- iOS: Xcode установлен (только macOS)
- Android: Android Studio установлен, эмулятор запущен

#### Вариант B: На физическом устройстве через Expo Go

1. Убедитесь, что телефон и компьютер в одной Wi-Fi сети
2. Запустите `npm start`
3. Отсканируйте QR код:
   - **iOS**: Откройте приложение Camera и наведите на QR код
   - **Android**: Откройте Expo Go и нажмите "Scan QR code"
4. Приложение загрузится на ваше устройство

#### Вариант C: Web версия (для тестирования)

```bash
npm start
# Затем нажмите 'w' в терминале
# Или
npm run web
```

### 7. Проверка работы

После запуска приложения:

1. ✅ Должен открыться экран входа (Login)
2. ✅ Можно зарегистрировать новый аккаунт
3. ✅ После входа должна открыться главная лента (Feed)

## 🔧 Решение проблем

### Проблема: "Unable to connect to Metro"

**Решение:**
```bash
# Очистить кэш и перезапустить
npm start -- --clear
```

### Проблема: "Network request failed" при подключении к API

**Причины и решения:**

1. **Backend не запущен**
   - Убедитесь, что backend работает на `http://localhost:3000`
   - Проверьте: `curl http://localhost:3000/api/health`

2. **Неправильный API URL для физического устройства**
   - Отредактируйте `mobile/src/config.ts`
   - Используйте IP адрес вашего компьютера
   - Убедитесь, что телефон и компьютер в одной сети

3. **Firewall блокирует подключение**
   - Разрешите подключения на порт 3000
   - macOS: System Preferences → Security → Firewall
   - Windows: Windows Defender Firewall

4. **Для Android эмулятора**
   - Убедитесь, что используется `10.0.2.2:3000` (уже настроено)
   - Не используйте `localhost` или `127.0.0.1`

### Проблема: "Expo Go не может подключиться"

**Решение:**
- Убедитесь, что телефон и компьютер в одной Wi-Fi сети
- Проверьте, что порт 8081 не заблокирован
- Попробуйте перезапустить: `npm start -- --clear`

### Проблема: Ошибки при установке зависимостей

**Решение:**
```bash
# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install

# Если проблема с Expo
npx expo install --fix
```

### Проблема: TypeScript ошибки

**Решение:**
```bash
# Проверить типы
npm run type-check

# Если нужно, переустановить типы
npm install --save-dev @types/react @types/react-native
```

## 📱 Полезные команды

```bash
# Запуск с очисткой кэша
npm start -- --clear

# Запуск на конкретной платформе
npm run ios          # iOS симулятор
npm run android      # Android эмулятор
npm run web          # Web браузер

# Проверка типов
npm run type-check

# Линтинг
npm run lint

# Перезапуск Metro bundler
# Нажмите 'r' в терминале Expo
```

## 🎯 Следующие шаги после запуска

1. ✅ Приложение запущено
2. ✅ Backend работает
3. 🔄 Зарегистрируйте тестовый аккаунт
4. 🔄 Протестируйте создание поста (когда будет реализовано)
5. 🔄 Протестируйте ленту

## 💡 Советы

- **Для разработки**: Используйте симулятор/эмулятор - быстрее и удобнее
- **Для тестирования**: Используйте физическое устройство через Expo Go
- **Hot Reload**: Изменения в коде автоматически применяются (нажмите 'r' если не работает)
- **Debugging**: Используйте React Native Debugger или Chrome DevTools
- **Логи**: Смотрите логи в терминале Expo или используйте `console.log()`

## 📚 Дополнительная информация

- [Expo Documentation](https://docs.expo.dev/)
- [React Navigation](https://reactnavigation.org/)
- [Troubleshooting Expo](https://docs.expo.dev/troubleshooting/clear-cache/)

---

**Готово!** Теперь вы можете разрабатывать HeirLink в Expo! 🎉
